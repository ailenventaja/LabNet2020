@model IEnumerable<Entities.Shippers>

<br>
<br>
<br>
<br>
<br>
<br>
<h2>Shippers</h2>
<br>
@using (Html.BeginForm("Index", "Shippers", FormMethod.Get))
{
    <div id="search">
       Find by company name: @Html.TextBox("search") <input type="submit" value="search" />
    </div>
}

<p>
    @Html.ActionLink("Create New", "Create")
</p>
@using (Html.BeginForm())
{

}
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CompanyName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Phone)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CompanyName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Phone)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.ShipperID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.ShipperID })
            </td>
        </tr>
    }

</table>
